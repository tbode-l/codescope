需要组员配合补充/确认：

1. 在 `tracker.py` 保存更多 GitHub 元数据：`forks`、`open_issues`、`updated_at`，方便后续评分更准确。
2. 在 `analysis.json` 里增加 `readme_path`，让 `scorer.py` 必要时可以读取原始 README。
3. 确认 `scores.json` 使用 `0-100` 分制，而不是接口契约里写的 `0-1`。
4. 统一 `tech_stack` 可选值：示例里有 `shell`，但白名单里没有，需要决定是否允许。
5. 确认 README 里的 `spec/competitor-tool-for-course-report.md` 等引用文件是否存在；如果不存在，改成当前实际文档路径。
6. 如果希望“重复运行只分析新增工具”成立，需要在分析完成后把仓库状态从 `NEW` 改成 `ANALYZED`。


当前仓库里还没有 analysis.json，所以没法直接试跑评分和出图；等 tracker.py/analyzer.py 跑出 analysis.json 后，再执行看看效果：